#ifndef KSYNC_PATH_CONFIG_H
#define KSYNC_PATH_CONFIG_H

#include <filesystem>

static const std::filesystem::path CMAKE_SOURCE_DIR = R"(${CMAKE_SOURCE_DIR})";
static const std::filesystem::path CMAKE_BINARY_DIR = R"(${CMAKE_BINARY_DIR})";
static const std::filesystem::path CMAKE_COMMAND = R"(${CMAKE_COMMAND})";
static const std::filesystem::path NGINX_COMMAND = R"(${NGINX})";
static const std::filesystem::path NGINX_CONF_TEMPLATE_DIR = R"(${NGINX_CONF_TEMPLATE_DIR})";
static const bool ZSYNC_SUPPORTED = ${ZSYNC_SUPPORTED};

#endif
